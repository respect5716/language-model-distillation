_target_: src.data.text.DataModule

defaults:
  - default

data_dir: ${data_dir}
pretrained_model_name_or_path: ${model.teacher.model_name_or_path}
mlm: ${model.mlm}
mlm_probability: 0.15

datasets:
  - namuwiki
  - modu-spoken
  - modu-written
  - modu-newspaper


