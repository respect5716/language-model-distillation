defaults:
  - default

_target_: src.data.text.DataModule

name: text
data_dir: ${data_dir}
teacher_name_or_model_path: ${model.teacher.name_or_model_path}

datasets:
  - namuwiki
  - modu-spoken
  - modu-written
  - modu-newspaper